{
  "name": "django-devbar-extension",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && node scripts/generate-icons.mjs && cp content.js devtools.html devtools.js panel.html panel.js privacy.html manifest.json dist/",
    "generate-icons": "node scripts/generate-icons.mjs"
  },
  "devDependencies": {
    "sharp": "^0.33.0"
  }
}
