{
  "name": "django-devbar-extension",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "build:dev": "node scripts/build.mjs --dev",
    "generate-icons": "node scripts/generate-icons.mjs",
    "zip": "cd dist && zip -r ../django-devbar-extension.zip ."
  },
  "devDependencies": {
    "sharp": "^0.34.5"
  }
}
